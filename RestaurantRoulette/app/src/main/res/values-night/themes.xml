<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RestaurantRoulette" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color1</item>
        <item name="colorPrimaryDark">@color/color2</item>
        <item name="colorAccent">@color/color2</item>
        <item name="android:textColor">@color/color3</item>
        <item name="android:windowBackground">@color/color1</item>
        <!-- Customize your theme here. -->
    </style>
</resources>